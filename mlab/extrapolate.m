t0 = 540;
t1 = 196608 / (2^16);
t2 = 393216 / (2^16);
t3 = 589824 / (2^16);
T = [
    1, 0, 0, 0;
    1, t1, t1^2, t1^3;
    1, t2, t2^2, t2^3;
    1, t3, t3^2, t3^3
]

x = [1.2; 1.4; 1.55; 1.62];
y = [1.3; 1.5; 1.9; 2.45];
scatter(x, y)

t = linspace(0, 12);
a=inv(T)*x;
x = a(1) + a(2).*t + a(3).*t.^2 + a(4).*t.^3;
a=inv(T)*y;
y = a(1) + a(2).*t + a(3).*t.^2 + a(4).*t.^3;
hold on
plot(x, y)
